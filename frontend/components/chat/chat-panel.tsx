"use client";

// Legacy sidebar chat panel — replaced by ChatBar (bottom-center layout).
// Kept for reference. Use ChatBar instead.

export function ChatPanel({ sessionId: _sessionId }: { sessionId: string }) {
  return null;
}
